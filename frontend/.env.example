# Local backend (Vite only reads vars prefixed with VITE_)
VITE_API_BASE_URL=http://localhost:5000

# Production (set this in Vercel → Settings → Environment Variables, then Redeploy):
# VITE_API_BASE_URL=https://resume-and-cover-letter-builder.onrender.com
